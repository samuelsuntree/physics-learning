---
import ChapterLayout from '../../layouts/ChapterLayout.astro';

const content = String.raw`<h1>第五章 全同粒子</h1>

  <p>
    当一个量子体系包含多个相同种类的粒子时，量子力学的数学框架中出现了一个深刻的新原理：
    <strong>全同粒子的不可分辨性</strong>。这一原理没有经典对应——经典力学中我们总可以"跟踪"每一个粒子的轨迹，
    即使粒子完全相同也能通过标号加以区分。但在量子力学中，全同粒子的不可分辨性是一条基本公设，
    它要求多粒子波函数具有特定的交换对称性：玻色子取对称波函数，费米子取反对称波函数。
    这一看似简单的对称性要求导致了极为丰富的物理后果——从 Pauli 不相容原理到元素周期表的结构，
    从白矮星的简并压到超流与超导现象。
  </p>

  <div class="insight-box"><div class="box-header insight-header"><svg class="box-icon" viewBox="0 0 20 20" fill="currentColor"><path d="M11 3a1 1 0 10-2 0v1a1 1 0 102 0V3zM15.657 5.757a1 1 0 00-1.414-1.414l-.707.707a1 1 0 001.414 1.414l.707-.707zM18 10a1 1 0 01-1 1h-1a1 1 0 110-2h1a1 1 0 011 1zM5.05 6.464A1 1 0 106.464 5.05l-.707-.707a1 1 0 00-1.414 1.414l.707.707zM4 11a1 1 0 100-2H3a1 1 0 000 2h1z" /><path fill-rule="evenodd" d="M10 5a5 5 0 00-3 9v2a1 1 0 001 1h4a1 1 0 001-1v-2a5 5 0 00-3-9z" clip-rule="evenodd" /></svg><span>全同性是量子力学的本质特征</span></div>
    <p>
      在经典力学中，两个质量和电荷完全相同的粒子仍可通过它们的轨迹加以区分——我们可以说
      "粒子 1 沿这条路径运动，粒子 2 沿那条路径运动"。但在量子力学中，粒子没有确定的轨迹。
      当两个电子的波函数在空间中重叠时，原则上不存在任何物理手段将它们区分开。
      因此，全同粒子的不可分辨性不是认识论上的困难，而是量子力学的<strong>本体论特征</strong>。
      正如 Feynman 所言："世界上所有的电子都是完全一样的。"
    </p>
  </div>

  <!-- ================================================================== -->
  <!-- 5.1 对称化与反对称化 -->
  <!-- ================================================================== -->
  <h2 id="symmetrization">5.1 对称化与反对称化</h2>

  <h3>全同粒子的不可分辨性公设</h3>

  <p>
    <strong>全同粒子</strong>是指内禀属性（质量、电荷、自旋等）完全相同的粒子。
    量子力学的<strong>不可分辨性公设</strong>指出：对于全同粒子组成的体系，
    交换任意两个粒子的所有坐标（包括空间坐标与自旋坐标）后，
    体系的一切物理可观测量不变。
  </p>

  <p>
    用数学语言表述：设 $\Psi(\mathbf{r}_1, \mathbf{r}_2)$ 是两个全同粒子的波函数（这里 $\mathbf r$ 泛指一切坐标），
    则交换粒子后的概率密度不变：
  </p>

  $$|\Psi(\mathbf r_1, \mathbf r_2)|^2 = |\Psi(\mathbf r_2, \mathbf r_1)|^2$$

  <p>
    这意味着 $\Psi(\mathbf{r}_1, \mathbf{r}_2)$ 与 $\Psi(\mathbf{r}_2, \mathbf{r}_1)$ 至多相差一个相位因子。
    下面我们将证明，这个相位因子只能是 $+1$ 或 $-1$。
  </p>

  <h3>交换算符及其性质</h3>

  <p>
    定义<strong>交换算符</strong>（permutation operator）$\hat P_{12}$：
  </p>

  $$\hat{P}_{12}\,\Psi(\mathbf{r}_1, \mathbf{r}_2) = \Psi(\mathbf{r}_2, \mathbf{r}_1)$$

  <p>显然，交换两次后回到原来的态，因此：</p>

  $$\hat{P}_{12}^2 = \hat{I}$$

  <details class="derivation-block"><summary class="derivation-toggle">推导交换算符的本征值</summary><div class="derivation-content">
    <p>
      将 $\hat P_{12}$ 连续作用两次：
    </p>
    <p>$$\hat{P}_{12}^2\,\Psi(\mathbf{r}_1, \mathbf{r}_2) = \hat{P}_{12}\,\Psi(\mathbf{r}_2, \mathbf{r}_1) = \Psi(\mathbf{r}_1, \mathbf{r}_2)$$</p>
    <p>
      因此 $$\hat{P}_{12}^2 = \hat{I}$$，即 $\hat P_{12}$ 是<strong>对合算符</strong>（involution）。
    </p>
    <p>
      设 $\hat{P}_{12}\,\Psi = \lambda\,\Psi$，则 $\hat P_{12}^2\Psi = \lambda^2\Psi = \Psi$，
      因此 $\lambda^2 = 1$，即 $\lambda = \pm 1$。
    </p>
    <p>
      此外，$\hat P_{12}$ 是 Hermite 算符且是幺正算符：$\hat{P}_{12}^\dagger = \hat{P}_{12}^{-1} = \hat{P}_{12}$。
      对于全同粒子，Hamilton 量在交换粒子下不变，因此 $[\hat{H}, \hat{P}_{12}] = 0$，
      交换对称性是运动常量——若初态具有确定的交换对称性（$+1$ 或 $-1$），
      该对称性在时间演化中保持不变。
    </p>
  </div></details>

  <p>
    本征值 $+1$ 对应<strong>对称态</strong>：$\hat{P}_{12}\,\Psi_S = +\Psi_S$。
    本征值 $-1$ 对应<strong>反对称态</strong>：$\hat{P}_{12}\,\Psi_A = -\Psi_A$。
  </p>

  <h3>玻色子与费米子</h3>

  <p>
    自然界中一切全同粒子严格遵守以下规则（自旋-统计定理）：
  </p>

  <ul>
    <li><strong>玻色子</strong>（Boson）：自旋为整数（$s = 0, 1, 2, \ldots$）的粒子，
      波函数在交换任意两个粒子时<strong>对称</strong>。例如：光子（$s = 1$）、$\pi$ 介子（$s = 0$）、$^4\text{He}$ 原子。</li>
    <li><strong>费米子</strong>（Fermion）：自旋为半整数（$s = 1/2, 3/2, 5/2, \ldots$）的粒子，
      波函数在交换任意两个粒子时<strong>反对称</strong>。例如：电子（$s = 1/2$）、质子、中子、夸克。</li>
  </ul>

  <p>
    对于 $N$ 个全同玻色子，波函数在任意置换下完全对称：
  </p>

  $$\Psi(\mathbf{r}_1, \mathbf{r}_2, \ldots, \mathbf{r}_N) = \Psi(\mathbf{r}_{P(1)}, \mathbf{r}_{P(2)}, \ldots, \mathbf{r}_{P(N)})$$

  <p>
    对于 $N$ 个全同费米子，波函数在任意置换下具有置换的奇偶性：
  </p>

  $$\Psi(\mathbf{r}_1, \mathbf{r}_2, \ldots, \mathbf{r}_N) = (-1)^p\,\Psi(\mathbf{r}_{P(1)}, \mathbf{r}_{P(2)}, \ldots, \mathbf{r}_{P(N)})$$

  <p>
    其中 $(-1)^p$ 是置换 $P$ 的<strong>符号</strong>（偶置换为 $+1$，奇置换为 $-1$）。
  </p>

  <div class="thinking-box"><div class="box-header thinking-header"><svg class="box-icon" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-8-3a1 1 0 00-.867.5 1 1 0 11-1.731-1A3 3 0 0113 8a3.001 3.001 0 01-2 2.83V11a1 1 0 11-2 0v-1a1 1 0 011-1 1 1 0 100-2zm0 8a1 1 0 100-2 1 1 0 000 2z" clip-rule="evenodd" /></svg><span>自旋-统计定理的深刻性</span></div>
    <p>
      自旋与统计性质之间的联系——整数自旋对应玻色统计，半整数自旋对应费米统计——在非相对论量子力学中
      只能作为公设引入，无法证明。其严格证明需要<strong>相对论性量子场论</strong>，
      是 Pauli 在 1940 年利用 Lorentz 不变性、微观因果性和能量正定性给出的。
      具体地说，如果试图将半整数自旋场量子化为玻色子（使用对易关系），
      会导致能量无下界（体系不稳定）；如果试图将整数自旋场量子化为费米子（使用反对易关系），
      会违反微观因果性（类空间隔处的场算符不对易）。因此自旋-统计联系是相对论和量子力学
      的共同要求，是自然界最深刻的定理之一。
    </p>
  </div>

  <h3>对称化算符与反对称化算符</h3>

  <p>
    给定一个（未经对称化的）$N$ 粒子波函数，可以用<strong>对称化算符</strong> $\hat S$ 和
    <strong>反对称化算符</strong> $\hat A$ 构造具有确定对称性的波函数：
  </p>

  $$\hat{S} = \frac{1}{N!}\sum_{P} \hat{P}$$
  $$\hat{A} = \frac{1}{N!}\sum_{P} (-1)^p\,\hat{P}$$

  <p>
    其中求和遍历 $N$ 个粒子的全部 $N!$ 个置换 $\hat{P}$，
    $p$ 是置换的逆序数。
  </p>

  <p>
    对于两粒子情形，这些算符简化为投影算符：
  </p>

  <p>
    对称化：$\Psi_S = \hat{S}\,\Psi = \frac{1}{2}(\Psi + \hat{P}_{12}\Psi)$
  </p>
  <p>
    反对称化：$\Psi_A = \hat{A}\,\Psi = \frac{1}{2}(\Psi - \hat{P}_{12}\Psi)$
  </p>

  <h3>Slater 行列式</h3>

  <p>
    对于费米子体系，反对称化的波函数可以用<strong>Slater 行列式</strong>简洁地表示。
    设 $N$ 个费米子分别占据单粒子态 $\psi_{\alpha_1}, \psi_{\alpha_2}, \ldots, \psi_{\alpha_N}$，
    则反对称化的 $N$ 体波函数为：
  </p>

  $$\Psi_A(\mathbf{r}_1, \ldots, \mathbf{r}_N) = \frac{1}{\sqrt{N!}}\begin{vmatrix} \psi_{\alpha_1}(\mathbf{r}_1) & \psi_{\alpha_2}(\mathbf{r}_1) & \cdots & \psi_{\alpha_N}(\mathbf{r}_1) \\ \psi_{\alpha_1}(\mathbf{r}_2) & \psi_{\alpha_2}(\mathbf{r}_2) & \cdots & \psi_{\alpha_N}(\mathbf{r}_2) \\ \vdots & \vdots & \ddots & \vdots \\ \psi_{\alpha_1}(\mathbf{r}_N) & \psi_{\alpha_2}(\mathbf{r}_N) & \cdots & \psi_{\alpha_N}(\mathbf{r}_N) \end{vmatrix}$$

  <p>
    其中 $1/\sqrt{N!}$ 为归一化因子。对于两粒子情形：
  </p>

  $$\Psi_A(\mathbf{r}_1, \mathbf{r}_2) = \frac{1}{\sqrt{2}}\begin{vmatrix} \psi_\alpha(\mathbf{r}_1) & \psi_\beta(\mathbf{r}_1) \\ \psi_\alpha(\mathbf{r}_2) & \psi_\beta(\mathbf{r}_2) \end{vmatrix}$$
  $$= \frac{1}{\sqrt{2}}\bigl[\psi_\alpha(\mathbf{r}_1)\psi_\beta(\mathbf{r}_2) - \psi_\beta(\mathbf{r}_1)\psi_\alpha(\mathbf{r}_2)\bigr]$$

  <p>
    Slater 行列式的数学结构自动保证了反对称性：交换两行（即交换两个粒子的坐标）
    使行列式变号；交换两列（即交换两个单粒子态的标号）也使行列式变号。
  </p>

  <h3>Pauli 不相容原理</h3>

  <p>
    <strong>Pauli 不相容原理</strong>是反对称性的直接推论：若两个费米子占据同一单粒子态
    （$\alpha_i = \alpha_j$），则 Slater 行列式有两列相同，
    $\Psi_A = 0$——波函数恒为零，这意味着该态不存在。
  </p>

  <p>
    换言之：<strong>不可能有两个全同费米子处于完全相同的量子态</strong>。
  </p>

  <div class="insight-box"><div class="box-header insight-header"><svg class="box-icon" viewBox="0 0 20 20" fill="currentColor"><path d="M11 3a1 1 0 10-2 0v1a1 1 0 102 0V3zM15.657 5.757a1 1 0 00-1.414-1.414l-.707.707a1 1 0 001.414 1.414l.707-.707zM18 10a1 1 0 01-1 1h-1a1 1 0 110-2h1a1 1 0 011 1zM5.05 6.464A1 1 0 106.464 5.05l-.707-.707a1 1 0 00-1.414 1.414l.707.707zM4 11a1 1 0 100-2H3a1 1 0 000 2h1z" /><path fill-rule="evenodd" d="M10 5a5 5 0 00-3 9v2a1 1 0 001 1h4a1 1 0 001-1v-2a5 5 0 00-3-9z" clip-rule="evenodd" /></svg><span>Pauli 原理的物理后果</span></div>
    <p>
      Pauli 不相容原理是理解物质结构最重要的原理之一。它决定了：
    </p>
    <ul>
      <li><strong>元素周期表</strong>：电子逐层填充原子轨道，每个轨道至多容纳两个自旋相反的电子（$m_s = \pm 1/2$），由此形成壳层结构和化学周期律。</li>
      <li><strong>白矮星与中子星</strong>：当引力将物质压缩到极端密度时，费米子的简并压（由不相容原理产生）抵抗进一步坍缩。白矮星由电子简并压支撑（Chandrasekhar 极限 $\sim 1.4\,M_\odot$），中子星由中子简并压支撑。</li>
      <li><strong>金属的导电性</strong>：自由电子在费米能以下逐级填充能级，形成费米海。只有费米面附近的电子才参与输运过程。</li>
      <li><strong>物质的稳定性</strong>：正是 Pauli 原理阻止了原子的坍缩——若电子是玻色子，所有电子将凝聚到最低能级，化学不复存在。</li>
    </ul>
  </div>

  <div class="reference-box"><div class="box-header ref-header"><span>参考教材</span></div>
    <ul>
      <li>Sakurai, <em>Modern Quantum Mechanics</em>, Ch.7.1-7.2：全同粒子与对称化公设</li>
      <li>Griffiths, <em>Introduction to Quantum Mechanics</em>, Ch.5.1：全同粒子</li>
      <li>曾谨言,《量子力学》卷I, 第八章 &sect;8.1-8.2：全同粒子与对称化原理</li>
    </ul>
  </div>

  <!-- ================================================================== -->
  <!-- 5.2 交换相互作用 -->
  <!-- ================================================================== -->
  <h2 id="exchange">5.2 交换相互作用</h2>

  <h3>两个电子的自旋-轨道波函数</h3>

  <p>
    考虑两个电子的体系。当自旋-轨道耦合可忽略时，总波函数可以近似分解为轨道部分与自旋部分的乘积：
  </p>

  <p>$\Psi(\mathbf{r}_1, \mathbf{r}_2) = \psi(\mathbf{r}_1, \mathbf{r}_2)\,\chi(s_1, s_2)$</p>

  <p>
    由于电子是费米子，总波函数必须在交换两个电子时反对称。这意味着只有两种允许的组合：
  </p>

  <ol>
    <li><strong>轨道对称 + 自旋反对称</strong>（$\psi_S \cdot \chi_{\text{singlet}}$）</li>
    <li><strong>轨道反对称 + 自旋对称</strong>（$\psi_A \cdot \chi_{\text{triplet}}$）</li>
  </ol>

  <h3>自旋单态与三重态</h3>

  <p>
    设两个电子分别占据单粒子轨道 $\psi_a$ 和 $\psi_b$（$a \neq b$）。
    对称和反对称的轨道波函数分别为：
  </p>

  $$\psi_S(\mathbf{r}_1, \mathbf{r}_2) = \frac{1}{\sqrt{2}}\bigl[\psi_a(\mathbf{r}_1)\psi_b(\mathbf{r}_2) + \psi_b(\mathbf{r}_1)\psi_a(\mathbf{r}_2)\bigr]$$
  $$\psi_A(\mathbf{r}_1, \mathbf{r}_2) = \frac{1}{\sqrt{2}}\bigl[\psi_a(\mathbf{r}_1)\psi_b(\mathbf{r}_2) - \psi_b(\mathbf{r}_1)\psi_a(\mathbf{r}_2)\bigr]$$

  <p>
    自旋部分有如下组合（利用第四章的结果）：
  </p>

  <p><strong>自旋单态</strong>（$S = 0$，反对称）：</p>
  $$\chi_{\text{singlet}} = \frac{1}{\sqrt{2}}\bigl(|\!\uparrow\downarrow\rangle - |\!\downarrow\uparrow\rangle\bigr), \quad S = 0$$

  <p><strong>自旋三重态</strong>（$S = 1$，对称），三个 $m_S$ 分量为：</p>
  <ul>
    <li>$m_S = +1$：$|\!\uparrow\uparrow\rangle$</li>
    <li>$m_S = 0$：$\frac{1}{\sqrt{2}}(|\!\uparrow\downarrow\rangle + |\!\downarrow\uparrow\rangle)$</li>
    <li>$m_S = -1$：$|\!\downarrow\downarrow\rangle$</li>
  </ul>

  <p>
    费米子反对称性要求：
    <strong>单态</strong>（自旋反对称）搭配<strong>对称轨道</strong> $\psi_S$；
    <strong>三重态</strong>（自旋对称）搭配<strong>反对称轨道</strong> $\psi_A$。
  </p>

  <h3>直接积分与交换积分</h3>

  <p>
    考虑两电子间的 Coulomb 排斥 $\hat V = e^2/|\mathbf r_1 - \mathbf r_2|$。
    在单态和三重态下，该排斥能的期望值分别为：
  </p>

  $$E_\pm = E_a + E_b + K \pm J$$

  <p>其中 $E_a$, $E_b$ 是单粒子能量，两个关键积分为：</p>

  <p><strong>直接积分</strong>（Coulomb integral）：</p>
  $$K = \int d^3r_1\,d^3r_2\;|\psi_a(\mathbf{r}_1)|^2\,\frac{e^2}{|\mathbf{r}_1 - \mathbf{r}_2|}\,|\psi_b(\mathbf{r}_2)|^2$$

  <p><strong>交换积分</strong>（exchange integral）：</p>
  $$J = \int d^3r_1\,d^3r_2\;\psi_a^*(\mathbf{r}_1)\psi_b^*(\mathbf{r}_2)\,\frac{e^2}{|\mathbf{r}_1 - \mathbf{r}_2|}\,\psi_b(\mathbf{r}_1)\psi_a(\mathbf{r}_2)$$

  <details class="derivation-block"><summary class="derivation-toggle">推导直接积分与交换积分</summary><div class="derivation-content">
    <p>
      以对称轨道波函数 $\psi_S$ 为例（对应单态），计算 Coulomb 排斥能的期望值：
    </p>
    <p>$$E_+ = \langle\psi_S|\hat{H}|\psi_S\rangle = E_a + E_b + \frac{1}{2}\bigl[(\langle ab| + \langle ba|)\hat{V}(|ab\rangle + |ba\rangle)\bigr]$$</p>
    <p>
      利用 $$\langle\psi_a\psi_b|\hat{V}|\psi_a\psi_b\rangle = K, \quad \langle\psi_a\psi_b|\hat{V}|\psi_b\psi_a\rangle = J$$，展开得：
    </p>
    <p>$$= E_a + E_b + \frac{1}{2}(K + J + J + K) = E_a + E_b + K + J$$</p>
    <p>
      类似地，反对称轨道波函数 $\psi_A$（对应三重态）给出：
    </p>
    <p>$$E_- = E_a + E_b + \frac{1}{2}(K - J - J + K) = E_a + E_b + K - J$$</p>
    <p>
      因此 $E_{\text{singlet}} = E_a + E_b + K + J$，$E_{\text{triplet}} = E_a + E_b + K - J$。
    </p>
    <p>
      <strong>直接积分</strong> $K$ 的物理意义是两个电子电荷分布
      $|\psi_a(\mathbf r_1)|^2$ 与 $|\psi_b(\mathbf r_2)|^2$ 之间的经典 Coulomb 排斥能。
      <strong>交换积分</strong> $J$ 则没有经典对应，它源于量子力学的全同性要求，
      涉及两个单粒子波函数的"交叉"重叠。
    </p>
  </div></details>

  <p>
    关键结论：单态与三重态的能量差为 $\Delta E = 2J$。
    对于 Coulomb 排斥，可以证明交换积分 $J > 0$
    （这对 Coulomb 势可以严格证明），因此 $E_{\text{triplet}} < E_{\text{singlet}}$——
    三重态能量更低。
  </p>

  <div class="thinking-box"><div class="box-header thinking-header"><svg class="box-icon" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-8-3a1 1 0 00-.867.5 1 1 0 11-1.731-1A3 3 0 0113 8a3.001 3.001 0 01-2 2.83V11a1 1 0 11-2 0v-1a1 1 0 011-1 1 1 0 100-2zm0 8a1 1 0 100-2 1 1 0 000 2z" clip-rule="evenodd" /></svg><span>交换积分的本质</span></div>
    <p>
      交换积分 $J$ 常被误解为一种新的"交换力"。实际上，体系中只有 Coulomb 相互作用，
      并没有新的力。能级劈裂 $2J$ 完全源于<strong>对称性约束</strong>对波函数空间部分的影响：
      反对称轨道波函数 $\psi_A$ 在 $\mathbf r_1 = \mathbf r_2$ 处为零（两电子不能在同一位置），
      这使得两电子的平均距离增大，从而降低了 Coulomb 排斥能。
      而对称轨道波函数 $\psi_S$ 在 $\mathbf r_1 = \mathbf r_2$ 处不为零，
      两电子平均距离更近，排斥能更高。因此三重态（反对称轨道）能量更低。
      "交换相互作用"本质上是<strong>对称性对空间关联的约束</strong>通过 Coulomb 相互作用产生的间接效应。
    </p>
  </div>

  <h3>氦原子能级：仲氦与正氦</h3>

  <p>
    氦原子有两个电子，其 Hamilton 量为：
  </p>

  $$\hat{H} = -\frac{\hbar^2}{2m}(\nabla_1^2 + \nabla_2^2) - \frac{Ze^2}{r_1} - \frac{Ze^2}{r_2} + \frac{e^2}{|\mathbf{r}_1 - \mathbf{r}_2|}$$

  <p>
    其中 $Z = 2$。氦原子的基态构型为 $1s^2$——
    两个电子都处于 $1s$ 轨道。由于两个电子占据相同的空间态 $\psi_{1s}$，
    轨道部分 $\psi_{1s}(\mathbf{r}_1)\psi_{1s}(\mathbf{r}_2)$ 自动对称，
    因此自旋部分必须反对称（单态，$S = 0$）：
  </p>

  $$\Psi_{1s^2} = \psi_{1s}(\mathbf{r}_1)\,\psi_{1s}(\mathbf{r}_2)\,\chi_{\text{singlet}}$$

  <p>
    对于<strong>激发态</strong>（构型 $1s\,nl$，其中一个电子被激发到 $nl$ 轨道），
    两个单粒子态不同，因此存在单态和三重态两种可能：
  </p>

  <ul>
    <li><strong>仲氦</strong>（para-helium）：自旋单态 $S = 0$，轨道对称，能量 $= E_{1s} + E_{nl} + K + J$</li>
    <li><strong>正氦</strong>（ortho-helium）：自旋三重态 $S = 1$，轨道反对称，能量 $= E_{1s} + E_{nl} + K - J$</li>
  </ul>

  <p>
    由于 $J > 0$，同一构型下正氦能级总是低于仲氦。
    这一能级分裂在光谱中清晰可辨——历史上正氦和仲氦曾被误认为是两种不同的元素。
    两者之间的电偶极跃迁被禁止（选择定则 $\Delta S = 0$），所以正氦态是亚稳态，寿命很长。
  </p>

  <h3>Hund 规则的物理起源</h3>

  <p>
    上述分析解释了原子物理中 <strong>Hund 第一规则</strong>的物理起源：
    <em>在给定电子构型下，总自旋 $S$ 最大的能级最低</em>。
  </p>

  <p>
    原因正是交换相互作用：总自旋 $S$ 越大，自旋部分越"对称"，轨道部分越"反对称"，
    电子间的平均距离越大，Coulomb 排斥能越低。形式上可以将交换效应写成等效的自旋-自旋相互作用：
  </p>

  $$\hat{H}_{\text{eff}} = \text{const} - 2J\,\hat{\mathbf{S}}_1 \cdot \hat{\mathbf{S}}_2$$

  <details class="derivation-block"><summary class="derivation-toggle">推导等效自旋 Hamilton 量</summary><div class="derivation-content">
    <p>
      利用恒等式：
    </p>
    <p>$$\hat{\mathbf{S}}_1 \cdot \hat{\mathbf{S}}_2 = \frac{1}{2}\bigl(\hat{S}^2 - \hat{S}_1^2 - \hat{S}_2^2\bigr)$$</p>
    <p>
      其中 $\hat S^2 = (\hat{\mathbf S}_1 + \hat{\mathbf S}_2)^2$。
      对于两个自旋-1/2 粒子，$\hat S_1^2 = \hat S_2^2 = \frac{3}{4}\hbar^2$，因此：
    </p>
    <p>
      单态（$S = 0$）：$\langle\hat{\mathbf{S}}_1\cdot\hat{\mathbf{S}}_2\rangle_{S=0} = -\frac{3\hbar^2}{4}$
    </p>
    <p>
      三重态（$S = 1$）：$\langle\hat{\mathbf{S}}_1\cdot\hat{\mathbf{S}}_2\rangle_{S=1} = +\frac{\hbar^2}{4}$
    </p>
    <p>
      能级差 $E_{\text{singlet}} - E_{\text{triplet}} = 2J$。
      注意到 $\langle\hat{\mathbf S}_1\cdot\hat{\mathbf S}_2\rangle$ 在单态与三重态之间的差为
      $\frac{1}{4}\hbar^2 - (-\frac{3}{4}\hbar^2) = \hbar^2$，
      因此可以构造等效 Hamilton 量 $$\hat{H}_{\text{eff}} = \text{const} - 2J\,\hat{\mathbf{S}}_1 \cdot \hat{\mathbf{S}}_2$$（取 $\hbar = 1$），
      它在两个自旋态上给出正确的能量差。
    </p>
    <p>
      这个等效 Hamilton 量就是<strong>Heisenberg 交换模型</strong>的基础。
      当 $J > 0$ 时，$\hat{\mathbf S}_1 \cdot \hat{\mathbf S}_2$ 取最大值（自旋平行）对应最低能量，
      倾向于<strong>铁磁序</strong>；当 $J < 0$ 时，自旋反平行能量最低，
      倾向于<strong>反铁磁序</strong>。
    </p>
  </div></details>

  <div class="insight-box"><div class="box-header insight-header"><svg class="box-icon" viewBox="0 0 20 20" fill="currentColor"><path d="M11 3a1 1 0 10-2 0v1a1 1 0 102 0V3zM15.657 5.757a1 1 0 00-1.414-1.414l-.707.707a1 1 0 001.414 1.414l.707-.707zM18 10a1 1 0 01-1 1h-1a1 1 0 110-2h1a1 1 0 011 1zM5.05 6.464A1 1 0 106.464 5.05l-.707-.707a1 1 0 00-1.414 1.414l.707.707zM4 11a1 1 0 100-2H3a1 1 0 000 2h1z" /><path fill-rule="evenodd" d="M10 5a5 5 0 00-3 9v2a1 1 0 001 1h4a1 1 0 001-1v-2a5 5 0 00-3-9z" clip-rule="evenodd" /></svg><span>从原子到固体：交换相互作用的广泛应用</span></div>
    <p>
      交换相互作用的概念远超原子物理范畴。在凝聚态物理中，
      相邻原子上电子的交换积分决定了磁性材料的基态——Heisenberg 模型
      $$\hat{H}_{\text{eff}} = \text{const} - 2J\,\hat{\mathbf{S}}_1 \cdot \hat{\mathbf{S}}_2$$ 是理解铁磁体、反铁磁体和自旋液体的出发点。
      在量子化学中，共价键的形成本质上也是交换效应的体现——
      两个氢原子的电子形成自旋单态（键合态），其轨道对称波函数
      使电子密度集中在两核之间，产生化学键。
      因此可以说：<strong>交换相互作用是化学的量子力学基础</strong>。
    </p>
  </div>

  <div class="reference-box"><div class="box-header ref-header"><span>参考教材</span></div>
    <ul>
      <li>Sakurai, <em>Modern Quantum Mechanics</em>, Ch.7.3-7.4：两电子体系与交换相互作用</li>
      <li>Griffiths, <em>Introduction to Quantum Mechanics</em>, Ch.5.2：原子（氦原子与交换力）</li>
      <li>曾谨言,《量子力学》卷I, 第八章 &sect;8.3-8.4：氦原子与交换积分</li>
    </ul>
  </div>

  <!-- ================================================================== -->
  <!-- 5.3 二次量子化初步 -->
  <!-- ================================================================== -->
  <h2 id="second-quantization">5.3 二次量子化初步</h2>

  <h3>为什么需要二次量子化？</h3>

  <p>
    在前两节中，我们用 Slater 行列式或对称化波函数处理全同粒子问题。
    这种方法（称为<strong>第一量子化</strong>）在粒子数较多时变得极为笨拙——
    $N$ 个费米子的 Slater 行列式包含 $N!$ 项，对称化/反对称化操作的计算量随粒子数指数增长。
    更根本的是，第一量子化无法自然地处理<strong>粒子数不确定</strong>的情形
    （如光子的发射与吸收、粒子的产生与湮灭）。
  </p>

  <p>
    <strong>二次量子化</strong>（second quantization）通过引入<strong>产生算符</strong>和<strong>湮灭算符</strong>，
    将全同粒子的对称性要求自动编码在代数关系中，极大地简化了多体问题的表述。
    这一形式是量子场论和凝聚态多体理论的基本语言。
  </p>

  <h3>产生算符与湮灭算符</h3>

  <p>
    选取一组完备正交归一的单粒子态 $\lbrace\phi_k\rbrace$（例如谐振子本征态、平面波等）。
    定义作用于单粒子态 $\phi_k$ 的<strong>产生算符</strong> $\hat{a}_k^\dagger$ 和
    <strong>湮灭算符</strong> $\hat{a}_k$，
    分别在态 $k$ 上增加或减少一个粒子。
  </p>

  <p>
    <strong>玻色子</strong>的产生湮灭算符满足<strong>对易关系</strong>：
  </p>

  $$[\hat{a}_k, \hat{a}_l^\dagger] = \delta_{kl}, \quad [\hat{a}_k, \hat{a}_l] = 0, \quad [\hat{a}_k^\dagger, \hat{a}_l^\dagger] = 0$$

  <p>
    <strong>费米子</strong>的产生湮灭算符满足<strong>反对易关系</strong>：
  </p>

  $$\{\hat{a}_k, \hat{a}_l^\dagger\} = \delta_{kl}, \quad \{\hat{a}_k, \hat{a}_l\} = 0, \quad \{\hat{a}_k^\dagger, \hat{a}_l^\dagger\} = 0$$

  <p>
    其中对易子 $[A, B] = AB - BA$，反对易子 $\lbrace A, B\rbrace = AB + BA$。
    两种代数关系的差异正是全同粒子对称性（玻色 vs. 费米）的代数体现。
  </p>

  <details class="derivation-block"><summary class="derivation-toggle">从反对易关系推导 Pauli 不相容原理</summary><div class="derivation-content">
    <p>
      对于费米子，反对易关系 $\lbrace\hat a_k, \hat a_l^\dagger\rbrace = \delta_{kl}$ 的一个直接推论是：
    </p>
    <p>$(\hat{a}_k^\dagger)^2 = 0$</p>
    <p>
      这是因为 $\lbrace\hat a_k^\dagger, \hat a_k^\dagger\rbrace = 2(\hat a_k^\dagger)^2 = 0$。
    </p>
    <p>
      物理意义：不能在同一单粒子态 $k$ 上产生两个费米子。
      因此每个态的占据数只能为 $n_k = 0$ 或 $1$——
      这正是 <strong>Pauli 不相容原理</strong>。
    </p>
    <p>
      产生和湮灭算符的具体作用为：
    </p>
    <p>$$\hat{a}_k^\dagger|0\rangle_k = |1\rangle_k, \quad \hat{a}_k^\dagger|1\rangle_k = 0$$</p>
    <p>$$\hat{a}_k|1\rangle_k = |0\rangle_k, \quad \hat{a}_k|0\rangle_k = 0$$</p>
    <p>
      对比玻色子：
    </p>
    <p>$$\hat{a}_k|n_k\rangle = \sqrt{n_k}\,|n_k - 1\rangle$$</p>
    <p>$$\hat{a}_k^\dagger|n_k\rangle = \sqrt{n_k + 1}\,|n_k + 1\rangle$$</p>
    <p>
      玻色子的占据数可以是任意非负整数 $n_k = 0, 1, 2, \ldots$，
      不受 Pauli 原理的限制。
    </p>
  </div></details>

  <h3>Fock 空间与粒子数表象</h3>

  <p>
    定义<strong>粒子数算符</strong>：
  </p>

  $$\hat{n}_k = \hat{a}_k^\dagger\,\hat{a}_k$$

  <p>其本征方程为 $$\hat{n}_k|n_k\rangle = n_k|n_k\rangle$$。
    对于玻色子，$n_k = 0, 1, 2, \ldots$；
    对于费米子，$n_k = 0, 1$。
  </p>

  <p>
    <strong>真空态</strong> $|0\rangle$ 定义为所有单粒子态都没有粒子的态：
    对所有 $k$，$\hat{a}_k|0\rangle = 0$。
    从真空态出发，用产生算符可以构造任意的多粒子态：
  </p>

  $$|n_1, n_2, \ldots\rangle = \prod_k \frac{(\hat{a}_k^\dagger)^{n_k}}{\sqrt{n_k!}}\,|0\rangle$$

  <p>
    全体这样的态（包括不同粒子数的态）张成的 Hilbert 空间称为 <strong>Fock 空间</strong>。
    以粒子数 $|n_1, n_2, \ldots\rangle$ 为基的表象称为<strong>粒子数表象</strong>
    （occupation number representation）。
  </p>

  <p>
    Fock 空间的总粒子数算符为：
  </p>

  $$\hat{N} = \sum_k \hat{n}_k = \sum_k \hat{a}_k^\dagger\,\hat{a}_k$$

  <p>
    Fock 空间的强大之处在于：它天然地包含不同粒子数的子空间，
    粒子数不再是固定的——产生算符增加粒子，湮灭算符减少粒子。
    这使得处理粒子产生和湮灭过程（如光的发射与吸收）成为可能。
  </p>

  <h3>单体算符与二体算符的二次量子化形式</h3>

  <p>
    在第一量子化中，一个<strong>单体算符</strong>（作用于每个粒子独立）的形式为：
  </p>

  $$\hat{F} = \sum_{i=1}^{N} \hat{f}(\mathbf{r}_i)$$

  <p>
    在二次量子化中，它表示为：
  </p>

  $$\hat{F} = \sum_{k,l} \langle k|\hat{f}|l\rangle\,\hat{a}_k^\dagger\,\hat{a}_l$$

  <p>
    其中矩阵元 $\langle k|\hat{f}|l\rangle = \int d^3r\;\phi_k^*(\mathbf{r})\,\hat{f}(\mathbf{r})\,\phi_l(\mathbf{r})$。
  </p>

  <p>
    类似地，<strong>二体算符</strong>（描述粒子间两两相互作用）在第一量子化中为：
  </p>

  $$\hat{G} = \frac{1}{2}\sum_{i \neq j} \hat{g}(\mathbf{r}_i, \mathbf{r}_j)$$

  <p>
    在二次量子化中表示为：
  </p>

  $$\hat{G} = \frac{1}{2}\sum_{k,l,m,n} \langle kl|\hat{g}|mn\rangle\,\hat{a}_k^\dagger\,\hat{a}_l^\dagger\,\hat{a}_n\,\hat{a}_m$$

  <p>
    其中矩阵元 $\langle kl|\hat{g}|mn\rangle = \int d^3r_1\,d^3r_2\;\phi_k^*(\mathbf{r}_1)\phi_l^*(\mathbf{r}_2)\,\hat{g}(\mathbf{r}_1, \mathbf{r}_2)\,\phi_m(\mathbf{r}_1)\phi_n(\mathbf{r}_2)$。
    注意二体算符中湮灭算符的顺序 $\hat{a}_k^\dagger\hat{a}_l^\dagger\hat{a}_n\hat{a}_m$——
    湮灭算符的排列顺序 $\hat a_n\hat a_m$（而非 $\hat a_m\hat a_n$）
    保证了<strong>正规序</strong>（normal ordering），避免了粒子"与自己相互作用"的非物理项。
  </p>

  <details class="derivation-block"><summary class="derivation-toggle">单体算符二次量子化形式的推导</summary><div class="derivation-content">
    <p>
      在第一量子化中，$N$ 粒子的单体算符为 $\hat F = \sum_i \hat f(\mathbf r_i)$。
      将单粒子态 $\phi_k(\mathbf{r})$ 作为完备基插入，
      对第 $i$ 个粒子的矩阵元为 $\langle k|\hat f|l\rangle$。
    </p>
    <p>
      在二次量子化中，算符 $\hat a_k^\dagger\hat a_l$ 的物理意义是：
      湮灭一个处于态 $l$ 的粒子，同时产生一个处于态 $k$ 的粒子——
      即实现从态 $l$ 到态 $k$ 的单粒子跃迁，幅度为 $\langle k|\hat f|l\rangle$。
      对所有 $k$, $l$ 求和就得到完整的单体算符。
    </p>
    <p>
      可以严格验证，二次量子化形式在任意粒子数 $N$ 的 Fock 空间子空间上，
      其矩阵元与第一量子化的结果完全一致。这保证了两种形式的等价性。
    </p>
  </div></details>

  <h3>场算符</h3>

  <p>
    定义<strong>场算符</strong>（field operator）：
  </p>

  $$\hat{\psi}(\mathbf{r}) = \sum_k \phi_k(\mathbf{r})\,\hat{a}_k$$
  $$\hat{\psi}^\dagger(\mathbf{r}) = \sum_k \phi_k^*(\mathbf{r})\,\hat{a}_k^\dagger$$

  <p>
    其中 $\phi_k(\mathbf{r})$ 是完备正交归一的单粒子基函数。
    场算符 $\hat\psi^\dagger(\mathbf r)$ 的物理意义是在位置 $\mathbf r$ 处产生一个粒子，
    $\hat\psi(\mathbf r)$ 是在位置 $\mathbf r$ 处湮灭一个粒子。
  </p>

  <p>场算符满足的（反）对易关系为：</p>

  <p>玻色子：</p>
  $$[\hat{\psi}(\mathbf{r}), \hat{\psi}^\dagger(\mathbf{r}')] = \delta^{(3)}(\mathbf{r} - \mathbf{r}')$$

  <p>费米子：</p>
  $$\{\hat{\psi}(\mathbf{r}), \hat{\psi}^\dagger(\mathbf{r}')\} = \delta^{(3)}(\mathbf{r} - \mathbf{r}')$$

  <details class="derivation-block"><summary class="derivation-toggle">场算符对易关系的推导</summary><div class="derivation-content">
    <p>
      以玻色子为例，利用场算符的定义和产生湮灭算符的对易关系：
    </p>
    <p>$$[\hat{\psi}(\mathbf{r}), \hat{\psi}^\dagger(\mathbf{r}')] = \sum_{k,l}\phi_k(\mathbf{r})\phi_l^*(\mathbf{r}')[\hat{a}_k, \hat{a}_l^\dagger] = \sum_k \phi_k(\mathbf{r})\phi_k^*(\mathbf{r}') = \delta^{(3)}(\mathbf{r} - \mathbf{r}')$$</p>
    <p>
      最后一步利用了单粒子基函数的完备性关系 $\sum_k \phi_k^*(\mathbf{r})\phi_k(\mathbf{r}') = \delta^{(3)}(\mathbf{r} - \mathbf{r}')$。
      费米子的情形完全类似，只需将对易子替换为反对易子。
    </p>
  </div></details>

  <p>
    用场算符，单体算符和二体算符有特别简洁的表达：
  </p>

  $$\hat{F} = \int d^3r\;\hat{\psi}^\dagger(\mathbf{r})\,\hat{f}(\mathbf{r})\,\hat{\psi}(\mathbf{r})$$
  $$\hat{G} = \frac{1}{2}\int d^3r_1\,d^3r_2\;\hat{\psi}^\dagger(\mathbf{r}_1)\hat{\psi}^\dagger(\mathbf{r}_2)\,\hat{g}(\mathbf{r}_1, \mathbf{r}_2)\,\hat{\psi}(\mathbf{r}_2)\hat{\psi}(\mathbf{r}_1)$$

  <details class="derivation-block"><summary class="derivation-toggle">验证场算符形式与离散形式的等价</summary><div class="derivation-content">
    <p>
      将场算符 $\hat\psi(\mathbf r) = \sum_k\phi_k(\mathbf r)\hat a_k$ 代入单体算符的场算符形式：
    </p>
    <p>$$\hat{F} = \sum_{k,l}\langle k|\hat{f}|l\rangle\,\hat{a}_k^\dagger\hat{a}_l = \int d^3r\sum_{k,l}\phi_k^*(\mathbf{r})\hat{f}(\mathbf{r})\phi_l(\mathbf{r})\,\hat{a}_k^\dagger\hat{a}_l = \int d^3r\;\hat{\psi}^\dagger(\mathbf{r})\hat{f}(\mathbf{r})\hat{\psi}(\mathbf{r})$$</p>
    <p>
      最后一步将 $\sum_k \phi_k^*(\mathbf r)\hat a_k^\dagger = \hat\psi^\dagger(\mathbf r)$ 和
      $\sum_l \phi_l(\mathbf r)\hat a_l = \hat\psi(\mathbf r)$ 代回，即得场算符形式。
      二体算符的验证类似，但需要注意算符的正规序排列。
    </p>
  </div></details>

  <p>
    体系的完整 Hamilton 量用场算符可以写成紧凑的形式：
  </p>

  $$\hat{H} = \int d^3r\;\hat{\psi}^\dagger(\mathbf{r})\left(-\frac{\hbar^2}{2m}\nabla^2 + V(\mathbf{r})\right)\hat{\psi}(\mathbf{r}) + \frac{1}{2}\int d^3r_1\,d^3r_2\;\hat{\psi}^\dagger(\mathbf{r}_1)\hat{\psi}^\dagger(\mathbf{r}_2)\,U(\mathbf{r}_1, \mathbf{r}_2)\,\hat{\psi}(\mathbf{r}_2)\hat{\psi}(\mathbf{r}_1)$$

  <p>
    其中第一项是单体部分（动能 + 外势），第二项是两体相互作用 $U(\mathbf r_1, \mathbf r_2)$。
    这一表达式的优美之处在于它<strong>不依赖于粒子数 $N$</strong>——
    粒子数通过场算符的 Fock 空间结构自动处理。
  </p>

  <h3>与第一量子化的等价性</h3>

  <p>
    二次量子化并不是一种"更深层"的量子化——对于粒子数固定的体系，
    它与第一量子化在数学上是<strong>完全等价</strong>的。具体地说：
  </p>

  <ol>
    <li>Fock 空间中粒子数为 $N$ 的子空间，与 $N$ 粒子对称化（或反对称化）的 Hilbert 空间同构。</li>
    <li>任何算符在二次量子化中的矩阵元，与第一量子化中用对称化/反对称化波函数计算的矩阵元完全相同。</li>
    <li>波函数的全同粒子对称性（玻色/费米）由对易/反对易关系自动保证，无需手动对称化。</li>
  </ol>

  <p>
    但二次量子化的真正优势在于：
  </p>

  <ul>
    <li><strong>形式简洁</strong>：无需写出显式的 Slater 行列式或对称化波函数。</li>
    <li><strong>粒子数可变</strong>：自然处理粒子产生/湮灭过程。</li>
    <li><strong>系统性近似</strong>：许多多体近似方法（Hartree-Fock、RPA、Green 函数方法等）在二次量子化框架中表述最为自然。</li>
    <li><strong>通向量子场论</strong>：将场算符视为基本自由度，是相对论性量子场论的出发点。</li>
  </ul>

  <div class="thinking-box"><div class="box-header thinking-header"><svg class="box-icon" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-8-3a1 1 0 00-.867.5 1 1 0 11-1.731-1A3 3 0 0113 8a3.001 3.001 0 01-2 2.83V11a1 1 0 11-2 0v-1a1 1 0 011-1 1 1 0 100-2zm0 8a1 1 0 100-2 1 1 0 000 2z" clip-rule="evenodd" /></svg><span>从量子力学到量子场论</span></div>
    <p>
      在非相对论量子力学中，二次量子化只是第一量子化的等价改写。
      但当我们转向相对论性理论时，情况发生了根本变化。
      相对论性理论（如 Dirac 方程）允许正负能解，
      负能解的物理解释要求粒子-反粒子对的产生和湮灭——
      粒子数不再守恒。此时第一量子化框架失效，
      而二次量子化（即量子场论）成为唯一正确的描述。
    </p>
    <p>
      从这个角度看，二次量子化中的场算符 $\hat\psi(\mathbf r)$
      不再仅仅是波函数的"算符化"，而是作为<strong>基本物理量</strong>的量子场——
      粒子是场的激发态。这一观念转变是从量子力学走向量子场论的关键一步：
      我们不再"量子化粒子"，而是"量子化场"。
    </p>
  </div>

  <div class="insight-box"><div class="box-header insight-header"><svg class="box-icon" viewBox="0 0 20 20" fill="currentColor"><path d="M11 3a1 1 0 10-2 0v1a1 1 0 102 0V3zM15.657 5.757a1 1 0 00-1.414-1.414l-.707.707a1 1 0 001.414 1.414l.707-.707zM18 10a1 1 0 01-1 1h-1a1 1 0 110-2h1a1 1 0 011 1zM5.05 6.464A1 1 0 106.464 5.05l-.707-.707a1 1 0 00-1.414 1.414l.707.707zM4 11a1 1 0 100-2H3a1 1 0 000 2h1z" /><path fill-rule="evenodd" d="M10 5a5 5 0 00-3 9v2a1 1 0 001 1h4a1 1 0 001-1v-2a5 5 0 00-3-9z" clip-rule="evenodd" /></svg><span>二次量子化的应用广度</span></div>
    <p>
      二次量子化是现代物理学中使用最广泛的形式语言之一：
    </p>
    <ul>
      <li><strong>凝聚态物理</strong>：电子气的 Hartree-Fock 理论、BCS 超导理论（Cooper 对 $\hat a_{k\uparrow}^\dagger\hat a_{-k\downarrow}^\dagger$）、Bose-Einstein 凝聚等。</li>
      <li><strong>量子光学</strong>：光场的量子化（$\hat a^\dagger$ 产生一个光子）、相干态 $|\alpha\rangle$、量子纠缠光源。</li>
      <li><strong>核物理</strong>：壳模型中的准粒子、核子配对等。</li>
      <li><strong>量子场论</strong>：将场本身作为算符处理，粒子是场的量子化激发。标准模型就建立在量子场论的框架之上。</li>
    </ul>
  </div>

  <div class="reference-box"><div class="box-header ref-header"><span>参考教材</span></div>
    <ul>
      <li>Sakurai, <em>Modern Quantum Mechanics</em>, Ch.7.5：二次量子化</li>
      <li>Griffiths, <em>Introduction to Quantum Mechanics</em>, Ch.5.4（附录）：二次量子化简介</li>
      <li>曾谨言,《量子力学》卷II, 第十一章：二次量子化方法</li>
      <li>A. L. Fetter & J. D. Walecka, <em>Quantum Theory of Many-Particle Systems</em>, Ch.1-2：二次量子化的系统处理</li>
    </ul>
  </div>

  <!-- ================================================================== -->
  <!-- 章末总结 -->
  <!-- ================================================================== -->
  <h2>本章小结</h2>

  <p>
    本章系统地讨论了全同粒子的量子力学理论，其核心要点如下：
  </p>

  <ol>
    <li>
      <strong>对称化公设</strong>：全同粒子的不可分辨性要求多粒子波函数具有确定的交换对称性。
      交换算符 $\hat P_{12}$ 满足 $\hat P_{12}^2 = I$，本征值为 $\pm 1$。
      玻色子（整数自旋）取对称波函数，费米子（半整数自旋）取反对称波函数。
      费米子的反对称性等价于 <strong>Pauli 不相容原理</strong>——不能有两个费米子占据同一量子态。
    </li>
    <li>
      <strong>交换相互作用</strong>：两电子体系中，全同性对波函数空间部分的约束通过 Coulomb 相互作用
      产生了能级劈裂 $\Delta E = 2J$（$J$ 为交换积分）。自旋三重态对应反对称轨道（电子远离），
      能量更低；自旋单态对应对称轨道（电子靠近），能量更高。
      这一机制解释了 Hund 规则和氦原子的仲氦/正氦谱线分裂。
    </li>
    <li>
      <strong>二次量子化</strong>：引入产生算符 $\hat a_k^\dagger$ 和湮灭算符 $\hat a_k$，
      将全同粒子的对称性编码在对易关系（玻色子）或反对易关系（费米子）中。
      Fock 空间以粒子数为基，自然处理粒子数可变的体系。
      单体和二体算符的二次量子化形式简洁统一。场算符
      $\hat\psi(\mathbf r) = \sum_k\phi_k(\mathbf r)\hat a_k$
      提供了坐标空间与 Fock 空间之间的桥梁。
    </li>
  </ol>

  <p>
    全同粒子理论是从单体量子力学走向多体物理的关键一步。
    它不仅是原子物理和分子物理的基础（元素周期表、化学键），
    更是凝聚态物理（费米液体、超导、超流）和高能物理（量子场论）的出发点。
    在后续课程中，微扰论、变分法等近似方法将在全同粒子体系上得到广泛应用。
  </p>

  <div class="thinking-box"><div class="box-header thinking-header"><svg class="box-icon" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-8-3a1 1 0 00-.867.5 1 1 0 11-1.731-1A3 3 0 0113 8a3.001 3.001 0 01-2 2.83V11a1 1 0 11-2 0v-1a1 1 0 011-1 1 1 0 100-2zm0 8a1 1 0 100-2 1 1 0 000 2z" clip-rule="evenodd" /></svg><span>全同性与量子信息</span></div>
    <p>
      全同粒子的量子统计性质在量子信息中也扮演重要角色。
      玻色子倾向于"聚集"在同一量子态（玻色增强），这是激光工作的原理，
      也是 Hong-Ou-Mandel 效应（两个全同光子在分束器上总是一起出射）的物理起源。
      费米子则"排斥"同一态，产生反聚束效应。
      这些效应在量子模拟和量子计算中有重要应用——
      例如利用超冷费米原子模拟 Hubbard 模型来研究高温超导机制。
      此外，全同粒子的纠缠概念（如何定义和量化全同粒子间的纠缠）
      本身就是量子信息理论中尚在活跃研究的前沿课题。
    </p>
  </div>`;
---

<ChapterLayout title="第五章 全同粒子" courseId="qm" chapterId="ch05">
  <Fragment set:html={content} />
</ChapterLayout>
