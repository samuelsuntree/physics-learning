---
import ChapterLayout from '../../layouts/ChapterLayout.astro';
import InsightBox from '../../components/InsightBox.astro';
import ThinkingBox from '../../components/ThinkingBox.astro';
import Derivation from '../../components/Derivation.astro';
import ReferenceBox from '../../components/ReferenceBox.astro';

const defined = {
  /* ── helpers: every LaTeX snippet that contains { or } lives here ── */

  /* §10.1 EPR 佯谬 */
  singlet: String.raw`$$|\Psi^-\rangle = \frac{1}{\sqrt{2}}\bigl(|\!\uparrow\downarrow\rangle - |\!\downarrow\uparrow\rangle\bigr) = \frac{1}{\sqrt{2}}\bigl(|+\rangle_A|-\rangle_B - |-\rangle_A|+\rangle_B\bigr)$$`,
  singletShort: String.raw`$|\Psi^-\rangle = \frac{1}{\sqrt{2}}(|\!\uparrow\downarrow\rangle - |\!\downarrow\uparrow\rangle)$`,
  singletInline: String.raw`$|\Psi^-\rangle$`,
  spinMeasureA: String.raw`$\hat{\sigma}_z^A$`,
  spinMeasureB: String.raw`$\hat{\sigma}_z^B$`,
  correlationQM: String.raw`$$\langle\Psi^-|\hat{\sigma}_{\mathbf{a}}^A \otimes \hat{\sigma}_{\mathbf{b}}^B|\Psi^-\rangle = -\mathbf{a}\cdot\mathbf{b} = -\cos\theta_{ab}$$`,
  sigmaA: String.raw`$\hat{\sigma}_{\mathbf{a}}^A = \mathbf{a}\cdot\hat{\boldsymbol{\sigma}}^A$`,
  sigmaB: String.raw`$\hat{\sigma}_{\mathbf{b}}^B = \mathbf{b}\cdot\hat{\boldsymbol{\sigma}}^B$`,
  singletAnyBasis: String.raw`$$|\Psi^-\rangle = \frac{1}{\sqrt{2}}\bigl(|+\hat{\mathbf{n}}\rangle_A|-\hat{\mathbf{n}}\rangle_B - |-\hat{\mathbf{n}}\rangle_A|+\hat{\mathbf{n}}\rangle_B\bigr)$$`,
  totalSpin: String.raw`$\hat{\mathbf{S}}_A + \hat{\mathbf{S}}_B = 0$`,
  bellStates: String.raw`$$|\Phi^{\pm}\rangle = \frac{1}{\sqrt{2}}(|\!\uparrow\uparrow\rangle \pm |\!\downarrow\downarrow\rangle), \quad |\Psi^{\pm}\rangle = \frac{1}{\sqrt{2}}(|\!\uparrow\downarrow\rangle \pm |\!\downarrow\uparrow\rangle)$$`,

  /* §10.1 关联推导 */
  corrDerivStep1: String.raw`$$\hat{\sigma}_{\mathbf{a}} = a_x\hat{\sigma}_x + a_y\hat{\sigma}_y + a_z\hat{\sigma}_z$$`,
  corrDerivStep2: String.raw`$$\hat{\sigma}_{\mathbf{a}}^A|\pm\hat{\mathbf{a}}\rangle_A = \pm|\pm\hat{\mathbf{a}}\rangle_A$$`,
  corrDerivStep3: String.raw`$$\langle\Psi^-|\hat{\sigma}_{\mathbf{a}}^A \otimes \hat{\sigma}_{\mathbf{b}}^B|\Psi^-\rangle = \frac{1}{2}\bigl[\langle +\hat{\mathbf{a}}|-\hat{\mathbf{a}}\rangle\langle -\hat{\mathbf{b}}|+\hat{\mathbf{b}}\rangle + \langle -\hat{\mathbf{a}}|+\hat{\mathbf{a}}\rangle\langle +\hat{\mathbf{b}}|-\hat{\mathbf{b}}\rangle\bigr] \cdot (+1)(-1) + \cdots$$`,
  corrDerivResult: String.raw`$$= -\frac{1}{2}\bigl[\langle +_{\mathbf{a}}|+_{\mathbf{a}}\rangle\langle -_{\mathbf{b}}|-_{\mathbf{b}}\rangle + \langle -_{\mathbf{a}}|-_{\mathbf{a}}\rangle\langle +_{\mathbf{b}}|+_{\mathbf{b}}\rangle\bigr] = -\mathbf{a}\cdot\mathbf{b}$$`,
  tensorProduct: String.raw`$\hat{\sigma}_{\mathbf{a}}^A \otimes \hat{\sigma}_{\mathbf{b}}^B$`,
  nhatBasis: String.raw`$\hat{\mathbf{n}}$`,

  /* §10.2 Bell 不等式 */
  hiddenVarA: String.raw`$A(\mathbf{a}, \lambda) = \pm 1$`,
  hiddenVarB: String.raw`$B(\mathbf{b}, \lambda) = \pm 1$`,
  rhoLambda: String.raw`$\rho(\lambda) \geq 0$`,
  rhoNorm: String.raw`$\int d\lambda\;\rho(\lambda) = 1$`,
  localCorr: String.raw`$$C(\mathbf{a}, \mathbf{b}) = \int d\lambda\;\rho(\lambda)\,A(\mathbf{a}, \lambda)\,B(\mathbf{b}, \lambda)$$`,
  CHSHdef: String.raw`$$S = C(\mathbf{a}, \mathbf{b}) - C(\mathbf{a}, \mathbf{b}') + C(\mathbf{a}', \mathbf{b}) + C(\mathbf{a}', \mathbf{b}')$$`,
  CHSHbound: String.raw`$$|S| \leq 2$$`,
  CHSHderiv1: String.raw`$$S = \int d\lambda\;\rho(\lambda)\left[A(\mathbf{a},\lambda)\bigl(B(\mathbf{b},\lambda) - B(\mathbf{b}',\lambda)\bigr) + A(\mathbf{a}',\lambda)\bigl(B(\mathbf{b},\lambda) + B(\mathbf{b}',\lambda)\bigr)\right]$$`,
  CHSHderiv2: String.raw`$B(\mathbf{b},\lambda) = \pm 1$`,
  CHSHderiv3: String.raw`$|B(\mathbf{b},\lambda) - B(\mathbf{b}',\lambda)| + |B(\mathbf{b},\lambda) + B(\mathbf{b}',\lambda)| = 2$`,
  CHSHderiv4: String.raw`$$|S| \leq \int d\lambda\;\rho(\lambda)\left[|B(\mathbf{b},\lambda) - B(\mathbf{b}',\lambda)| + |B(\mathbf{b},\lambda) + B(\mathbf{b}',\lambda)|\right] = 2$$`,

  /* 量子力学预言 */
  SQM: String.raw`$$S_{\text{QM}} = -\cos\theta_{ab} + \cos\theta_{ab'} - \cos\theta_{a'b} - \cos\theta_{a'b'}$$`,
  optimalAngles: String.raw`$\mathbf{a} = \hat{z},\;\mathbf{a}' = \hat{x},\;\mathbf{b} = (\hat{z}+\hat{x})/\sqrt{2},\;\mathbf{b}' = (\hat{z}-\hat{x})/\sqrt{2}$`,
  thetaValues: String.raw`$\theta_{ab} = \theta_{a'b} = \theta_{a'b'} = \pi/4,\;\theta_{ab'} = 3\pi/4$`,
  tsirelson: String.raw`$$|S_{\text{QM}}|_{\max} = 2\sqrt{2} \approx 2.828$$`,
  tsirelsonInline: String.raw`$2\sqrt{2}$`,
  SQMmax: String.raw`$$S_{\text{QM}} = -\cos\frac{\pi}{4} + \cos\frac{3\pi}{4} - \cos\frac{\pi}{4} - \cos\frac{\pi}{4} = -\frac{\sqrt{2}}{2} - \frac{\sqrt{2}}{2} - \frac{\sqrt{2}}{2} - \frac{\sqrt{2}}{2} = -2\sqrt{2}$$`,

  /* 实验 */
  detectionLoophole: String.raw`$\eta > 2/(1 + \sqrt{2}) \approx 82.8\%$`,

  /* §10.3 量子信息 */
  qubitState: String.raw`$$|\psi\rangle = \alpha|0\rangle + \beta|1\rangle, \quad |\alpha|^2 + |\beta|^2 = 1$$`,
  blochSphere: String.raw`$$|\psi\rangle = \cos\frac{\theta}{2}|0\rangle + e^{i\varphi}\sin\frac{\theta}{2}|1\rangle$$`,
  blochVector: String.raw`$\hat{\mathbf{n}} = (\sin\theta\cos\varphi,\;\sin\theta\sin\varphi,\;\cos\theta)$`,
  densityPure: String.raw`$\hat{\rho} = |\psi\rangle\langle\psi|$`,
  densityMixed: String.raw`$$\hat{\rho} = \frac{1}{2}(\hat{I} + \mathbf{r}\cdot\hat{\boldsymbol{\sigma}}), \quad |\mathbf{r}| \leq 1$$`,
  blochPure: String.raw`$|\mathbf{r}| = 1$`,
  blochMixed: String.raw`$|\mathbf{r}| < 1$`,
  blochCenter: String.raw`$\mathbf{r} = 0$`,

  /* 不可克隆 */
  cloningAssume: String.raw`$$\hat{U}|\psi\rangle|0\rangle = |\psi\rangle|\psi\rangle$$`,
  cloningPsi: String.raw`$|\psi\rangle$`,
  cloningPhi: String.raw`$|\phi\rangle$`,
  cloningU1: String.raw`$$\hat{U}|\psi\rangle|0\rangle = |\psi\rangle|\psi\rangle$$`,
  cloningU2: String.raw`$$\hat{U}|\phi\rangle|0\rangle = |\phi\rangle|\phi\rangle$$`,
  cloningInner: String.raw`$$\langle\phi|\psi\rangle = \langle\phi|\psi\rangle^2$$`,
  cloningContradiction: String.raw`$\langle\phi|\psi\rangle(1 - \langle\phi|\psi\rangle) = 0$`,

  /* 隐形传态 */
  teleportInitial: String.raw`$$|\Phi\rangle = |\psi\rangle_1 \otimes |\Psi^-\rangle_{23} = (\alpha|0\rangle_1 + \beta|1\rangle_1)\otimes\frac{1}{\sqrt{2}}(|01\rangle_{23} - |10\rangle_{23})$$`,
  bellBasis: String.raw`$$|0\rangle_1|0\rangle_2 = \frac{1}{\sqrt{2}}(|\Phi^+\rangle_{12} + |\Phi^-\rangle_{12}), \quad |0\rangle_1|1\rangle_2 = \frac{1}{\sqrt{2}}(|\Psi^+\rangle_{12} + |\Psi^-\rangle_{12})$$`,
  bellBasis2: String.raw`$$|1\rangle_1|0\rangle_2 = \frac{1}{\sqrt{2}}(|\Psi^+\rangle_{12} - |\Psi^-\rangle_{12}), \quad |1\rangle_1|1\rangle_2 = \frac{1}{\sqrt{2}}(|\Phi^+\rangle_{12} - |\Phi^-\rangle_{12})$$`,
  teleportExpand: String.raw`$$|\Phi\rangle = \frac{1}{2}\bigl[|\Psi^-\rangle_{12}(-\alpha|0\rangle_3 - \beta|1\rangle_3) + |\Psi^+\rangle_{12}(-\alpha|0\rangle_3 + \beta|1\rangle_3) + |\Phi^-\rangle_{12}(\alpha|1\rangle_3 + \beta|0\rangle_3) + |\Phi^+\rangle_{12}(\alpha|1\rangle_3 - \beta|0\rangle_3)\bigr]$$`,
  teleportResult: String.raw`$|\psi\rangle_3 = \alpha|0\rangle_3 + \beta|1\rangle_3$`,
  pauliCorrections: String.raw`$\hat{I},\;\hat{\sigma}_z,\;\hat{\sigma}_x,\;i\hat{\sigma}_y$`,

  /* 退相干 */
  systemEnv: String.raw`$$|\Psi\rangle_{SE} = \sum_i c_i|s_i\rangle \otimes |e_i\rangle$$`,
  reducedDensity: String.raw`$$\hat{\rho}_S = \text{Tr}_E\,|\Psi\rangle_{SE}\langle\Psi| = \sum_{ij}c_i c_j^*\langle e_j|e_i\rangle\,|s_i\rangle\langle s_j|$$`,
  decoherence: String.raw`$\langle e_j|e_i\rangle \to \delta_{ij}$`,
  decoherentRho: String.raw`$$\hat{\rho}_S \to \sum_i |c_i|^2\,|s_i\rangle\langle s_i|$$`,
  vonNeumann: String.raw`$S(\hat{\rho}) = -\text{Tr}(\hat{\rho}\ln\hat{\rho})$`,
  pureEntropy: String.raw`$S = 0$`,
  maxEntropy: String.raw`$S = \ln d$`,
  entanglementEntropy: String.raw`$$S(\hat{\rho}_A) = -\text{Tr}(\hat{\rho}_A\ln\hat{\rho}_A)$$`,
  schmidtDecomp: String.raw`$$|\Psi\rangle_{AB} = \sum_i \lambda_i |a_i\rangle_A|b_i\rangle_B, \quad \lambda_i \geq 0,\;\sum_i \lambda_i^2 = 1$$`,

  /* 通用 */
  hbar: String.raw`$\hbar$`,
  ketZero: String.raw`$|0\rangle$`,
  ketOne: String.raw`$|1\rangle$`,
};
---

<ChapterLayout title="第十章 量子纠缠与Bell不等式" courseId="qm" chapterId="ch10">

  <h1>第十章 量子纠缠与Bell不等式</h1>

  <p>
    量子纠缠是量子力学中最具颠覆性的概念之一。Einstein 将其称为"鬼魅般的超距作用"
    （spooky action at a distance），并以此质疑量子力学的完备性。
    然而，Bell 于 1964 年证明了一个深刻的定理：任何试图用定域隐变量来"完备化"量子力学的理论，
    都必然对某些可观测量的关联函数给出比量子力学更弱的预言——并且这一差异是可以实验检验的。
    从 Aspect（1982）到一系列消除各种漏洞的精密实验，自然界反复确认了量子力学的预言：
    Bell 不等式被违反，自然界的关联超越了任何定域实在论模型所能解释的范围。
    本章从 EPR 佯谬出发，严格推导 Bell 不等式及其量子力学违反，
    并简要介绍由量子纠缠引发的量子信息科学的基本概念。
  </p>

  <!-- ================================================================== -->
  <!-- 10.1 EPR 佯谬 -->
  <!-- ================================================================== -->
  <h2 id="epr">10.1 EPR 佯谬</h2>

  <h3>EPR 论证</h3>

  <p>
    1935 年，Einstein、Podolsky 和 Rosen（EPR）发表了一篇影响深远的论文，
    对量子力学的完备性提出质疑。他们的论证基于三个看似合理的假设：
  </p>

  <ol>
    <li>
      <strong>完备性判据</strong>：一个完备的物理理论应当对物理实在的每一个元素都有对应的描述。
    </li>
    <li>
      <strong>实在性判据</strong>：如果不对系统进行任何扰动就能确定地预言一个物理量的值，
      则存在一个物理实在的元素与该物理量对应。
    </li>
    <li>
      <strong>定域性</strong>：对一个系统进行的测量不能瞬时影响空间上远离的另一个系统。
    </li>
  </ol>

  <h3>自旋单态与 EPR 关联</h3>

  <p>
    EPR 论证最清晰的版本（由 Bohm 于 1951 年提出）使用两个自旋-1/2 粒子的自旋单态：
  </p>

  <Fragment set:html={defined.singlet} />

  <p>
    这是一个总自旋为零的态（<Fragment set:html={defined.totalSpin} />），具有一个非凡的性质：
    它在<strong>任意</strong>自旋量子化轴下都保持相同的形式。
    即对任意方向 <Fragment set:html={defined.nhatBasis} />：
  </p>

  <Fragment set:html={defined.singletAnyBasis} />

  <p>
    考虑以下实验：两个粒子相向飞出，Alice 和 Bob 分别在远处测量各自粒子的自旋。
    Alice 测量方向 $\mathbf{a}$ 上的自旋 <Fragment set:html={defined.sigmaA} />，
    Bob 测量方向 $\mathbf{b}$ 上的自旋 <Fragment set:html={defined.sigmaB} />。
    量子力学预言两者的关联为
  </p>

  <Fragment set:html={defined.correlationQM} />

  <p>
    其中 $\theta_{ab}$ 是 $\mathbf{a}$ 与 $\mathbf{b}$ 之间的夹角。
    特别地，当 $\mathbf{a} = \mathbf{b}$ 时（同方向测量），
    关联为 $-1$——Alice 和 Bob 的结果一定相反。
  </p>

  <Derivation title="自旋单态关联函数的推导">
    <p>
      需要计算 <Fragment set:html={defined.tensorProduct} /> 在单态 <Fragment set:html={defined.singletInline} /> 上的期望值。
      利用单态在任意基下的不变性，选取 $\mathbf{a}$ 作为量子化轴。
      在 $\mathbf{a}$ 基下，单态为
    </p>
    $$|\Psi^-\rangle = \frac{1}{\sqrt{2}}(|+_{\mathbf{a}}\rangle_A|-_{\mathbf{a}}\rangle_B - |-_{\mathbf{a}}\rangle_A|+_{\mathbf{a}}\rangle_B)$$
    <p>
      由于 <Fragment set:html={defined.corrDerivStep2} />，
      粒子 A 在 $\mathbf{a}$ 方向的测量结果分别为 $\pm 1$。
      粒子 B 在 $\mathbf{b}$ 方向上：$\hat{\sigma}_{\mathbf{b}}|\pm_{\mathbf{a}}\rangle$
      需要利用 $|\pm_{\mathbf{a}}\rangle$ 在 $\mathbf{b}$ 基下的分解。
      直接计算：
    </p>
    $$\langle\Psi^-|\hat{\sigma}_{\mathbf{a}}^A\hat{\sigma}_{\mathbf{b}}^B|\Psi^-\rangle = \frac{1}{2}\bigl[(+1)\langle -_{\mathbf{a}}|\hat{\sigma}_{\mathbf{b}}|-_{\mathbf{a}}\rangle + (-1)\langle +_{\mathbf{a}}|\hat{\sigma}_{\mathbf{b}}|+_{\mathbf{a}}\rangle\bigr]$$
    <p>
      利用 $\langle\pm_{\mathbf{a}}|\hat{\sigma}_{\mathbf{b}}|\pm_{\mathbf{a}}\rangle = \pm\mathbf{a}\cdot\mathbf{b}$
      （$\hat{\sigma}_{\mathbf{b}}$ 在 $\mathbf{a}$ 方向本征态上的期望值即为 $\mathbf{b}$ 在 $\mathbf{a}$ 方向的投影），
      得到
    </p>
    $$\langle\Psi^-|\hat{\sigma}_{\mathbf{a}}^A\hat{\sigma}_{\mathbf{b}}^B|\Psi^-\rangle = \frac{1}{2}[(-\mathbf{a}\cdot\mathbf{b}) - (+\mathbf{a}\cdot\mathbf{b})] = -\mathbf{a}\cdot\mathbf{b}$$
    <p>
      这就是关联函数 $C(\mathbf{a}, \mathbf{b}) = -\cos\theta_{ab}$ 的来源。
    </p>
  </Derivation>

  <h3>EPR 的"悖论"</h3>

  <p>
    EPR 的论证如下：当 $\mathbf{a} = \mathbf{b}$ 时，Alice 测量 $\hat{\sigma}_z$
    得到 $+1$，则她能确定地预言 Bob 的 $\hat{\sigma}_z$ 结果为 $-1$——
    不需要对 Bob 的粒子做任何操作。根据实在性判据，
    Bob 粒子的 $\hat{\sigma}_z$ 值在测量前就已经确定。
    但 Alice 同样可以选择测量 $\hat{\sigma}_x$，由此确定 Bob 粒子的 $\hat{\sigma}_x$ 值。
    根据定域性假设，Alice 对自己粒子的测量选择不能影响 Bob 的粒子——
    因此 Bob 粒子的 $\hat{\sigma}_z$ 和 $\hat{\sigma}_x$ 值<strong>同时</strong>确定。
    但量子力学中 $\hat{\sigma}_z$ 和 $\hat{\sigma}_x$ 不对易，
    不可能同时具有确定值。EPR 由此得出结论：量子力学是<strong>不完备</strong>的——
    必须存在某些"隐变量"来补充量子力学的描述。
  </p>

  <InsightBox title="EPR 与量子纠缠">
    <p>
      EPR 论证的核心在于单态 <Fragment set:html={defined.singletInline} /> 的非定域关联。
      从现代视角看，这种关联正是<strong>量子纠缠</strong>的体现——
      两个粒子的量子态不能写成各自量子态的直积，它们构成一个不可分割的整体。
      Bell 态（四个最大纠缠态）
    </p>
    <Fragment set:html={defined.bellStates} />
    <p>
      构成两量子比特 Hilbert 空间的一组正交基（Bell 基），是量子信息理论中最基本的资源态。
      EPR 佯谬揭示了纠缠态的"非经典"本质，但要真正判断 Einstein 和 Bohr 谁对，
      需要一个可以实验检验的判据——这就是 Bell 不等式。
    </p>
  </InsightBox>

  <ReferenceBox>
    <ul>
      <li>Einstein, Podolsky, Rosen, <em>Phys. Rev.</em> <strong>47</strong>, 777 (1935)：EPR 原始论文</li>
      <li>Bohm, <em>Quantum Theory</em> (1951), Ch.22：自旋版 EPR 论证</li>
      <li>Sakurai, <em>Modern Quantum Mechanics</em>, Ch.3.9：EPR 佯谬和 Bell 不等式</li>
      <li>Nielsen &amp; Chuang, <em>Quantum Computation and Quantum Information</em>, Ch.2.6：EPR 与纠缠</li>
    </ul>
  </ReferenceBox>

  <!-- ================================================================== -->
  <!-- 10.2 Bell 不等式与实验检验 -->
  <!-- ================================================================== -->
  <h2 id="bell">10.2 Bell 不等式与实验检验</h2>

  <h3>定域隐变量理论</h3>

  <p>
    Bell（1964）天才地将 EPR 的哲学争论转化为一个可实验检验的数学不等式。
    假设存在一个定域隐变量理论：
  </p>

  <ul>
    <li>
      存在隐变量 $\lambda$（可以是任意维度的参数），描述粒子的"完备"状态，
      服从某个概率分布 <Fragment set:html={defined.rhoLambda} />（<Fragment set:html={defined.rhoNorm} />）。
    </li>
    <li>
      Alice 的测量结果 <Fragment set:html={defined.hiddenVarA} /> 仅取决于她的测量方向 $\mathbf{a}$ 和隐变量 $\lambda$
      （<strong>定域性</strong>：不依赖于 Bob 的测量方向 $\mathbf{b}$）。
    </li>
    <li>
      同理，Bob 的测量结果 <Fragment set:html={defined.hiddenVarB} /> 仅取决于 $\mathbf{b}$ 和 $\lambda$。
    </li>
  </ul>

  <p>
    在这样的框架中，关联函数为
  </p>

  <Fragment set:html={defined.localCorr} />

  <h3>CHSH 不等式</h3>

  <p>
    Clauser、Horne、Shimony 和 Holt（1969）给出了 Bell 不等式最实用的形式（CHSH 不等式）。
    考虑 Alice 的两个测量方向 $\mathbf{a}, \mathbf{a}'$ 和 Bob 的两个测量方向 $\mathbf{b}, \mathbf{b}'$，
    定义组合量
  </p>

  <Fragment set:html={defined.CHSHdef} />

  <p>
    则在<strong>任何</strong>定域隐变量理论中，必然满足
  </p>

  <Fragment set:html={defined.CHSHbound} />

  <Derivation title="CHSH 不等式的严格推导">
    <p>
      将 $S$ 用隐变量的期望值表示：
    </p>
    <Fragment set:html={defined.CHSHderiv1} />
    <p>
      关键步骤：对于每一个固定的 $\lambda$，$B(\mathbf{b}, \lambda)$ 和 $B(\mathbf{b}', \lambda)$
      各取 $\pm 1$。无论它们取何值，下面的恒等式成立：
    </p>
    <p>
      若 <Fragment set:html={defined.CHSHderiv2} />，则 $B$ 和 $B'$ 要么相同要么不同。
      当 $B = B'$ 时：$|B - B'| = 0$，$|B + B'| = 2$；
      当 $B = -B'$ 时：$|B - B'| = 2$，$|B + B'| = 0$。
      因此总有
    </p>
    <p><Fragment set:html={defined.CHSHderiv3} /></p>
    <p>
      这意味着对于每个 $\lambda$，被积函数中
      $|A(\mathbf{a},\lambda)(B - B')| + |A(\mathbf{a}',\lambda)(B + B')|
      \leq |B - B'| + |B + B'| = 2$
      （因为 $|A| = 1$）。
      因此
    </p>
    <Fragment set:html={defined.CHSHderiv4} />
    <p>
      证毕。注意这一证明只用到了三个条件：(1) 测量结果为 $\pm 1$；
      (2) 定域性（$A$ 不依赖 $\mathbf{b}$，$B$ 不依赖 $\mathbf{a}$）；
      (3) 结果由共同的隐变量 $\lambda$ 按概率分布 $\rho(\lambda)$ 决定。
      不需要对 $\lambda$ 的本质或 $\rho$ 的形式做任何假设。
    </p>
  </Derivation>

  <h3>量子力学的预言：Tsirelson 界</h3>

  <p>
    利用自旋单态的关联函数 $C(\mathbf{a}, \mathbf{b}) = -\cos\theta_{ab}$，
    量子力学对 CHSH 参量的预言为
  </p>

  <Fragment set:html={defined.SQM} />

  <p>
    通过选择最优测量方向——Alice 的两个方向 $\mathbf{a}, \mathbf{a}'$ 互相垂直，
    Bob 的两个方向 $\mathbf{b}, \mathbf{b}'$ 也互相垂直，
    且 Bob 的方向相对 Alice 旋转 $\pi/4$：
  </p>

  <p>
    <Fragment set:html={defined.optimalAngles} />
  </p>

  <p>
    此时 <Fragment set:html={defined.thetaValues} />，代入得
  </p>

  <Fragment set:html={defined.SQMmax} />

  <p>
    绝对值为 <Fragment set:html={defined.tsirelsonInline} /> $\approx 2.828$，明确违反了 CHSH 不等式的上界 $2$。
  </p>

  <p>
    Tsirelson（1980）证明了 <Fragment set:html={defined.tsirelsonInline} /> 是量子力学允许的<strong>最大值</strong>——
    无论选择什么量子态和什么测量，CHSH 参量不可能超过 <Fragment set:html={defined.tsirelsonInline} />：
  </p>

  <Fragment set:html={defined.tsirelson} />

  <p>
    因此量子力学的关联性强于定域隐变量理论（$|S| \leq 2$），但仍弱于数学上允许的最大值
    $|S| = 4$（无信号约束下的上界）。Tsirelson 界 <Fragment set:html={defined.tsirelsonInline} /> 的深层原因
    至今仍是量子信息基础研究的活跃课题。
  </p>

  <InsightBox title="Bell 定理的深刻含义">
    <p>
      Bell 不等式的违反意味着我们必须放弃以下三个假设中的至少一个：
      (1) 定域性——空间类似分隔的事件之间没有因果联系；
      (2) 实在性——物理量在测量前就有确定值；
      (3) 自由选择——实验者可以自由选择测量方向。
      主流物理学界的解读是放弃"定域实在论"——自然界确实存在非定域的量子关联。
      但需要强调的是，量子纠缠<strong>不能</strong>用于超光速通信——
      Alice 的局域测量结果的边缘概率分布不依赖于 Bob 的测量选择，
      这与相对论的因果性完全一致。非定域性体现在<strong>关联</strong>之中，
      而非单方面可观测的效应中。
    </p>
  </InsightBox>

  <h3>实验检验的历程</h3>

  <p>
    Bell 不等式的实验检验是 20 世纪物理学最重要的实验成就之一：
  </p>

  <ul>
    <li>
      <strong>Freedman &amp; Clauser (1972)</strong>：首次实验观测到 Bell 不等式的违反，
      使用钙原子级联辐射产生纠缠光子对。
    </li>
    <li>
      <strong>Aspect 实验 (1982)</strong>：关键性的改进——使用快速切换的偏振分析器，
      在光子飞行途中随机改变测量方向，排除了测量设备之间事先"串通"（定域性漏洞）的可能性。
      实验结果以高统计显著性违反 Bell 不等式，与量子力学预言完美一致。
    </li>
    <li>
      <strong>无漏洞实验 (2015)</strong>：Delft、Vienna 和 NIST 三个团队几乎同时报告了
      同时关闭<strong>定域性漏洞</strong>（测量事件空间类似分隔）和<strong>探测漏洞</strong>
      （探测效率足够高，<Fragment set:html={defined.detectionLoophole} />）的 Bell 实验。
      这些实验在最严格的条件下证实了 Bell 不等式的违反。
    </li>
  </ul>

  <p>
    2022 年诺贝尔物理学奖授予 Aspect、Clauser 和 Zeilinger，
    表彰他们"用纠缠光子进行的实验，确立了 Bell 不等式的违反并开创了量子信息科学"。
  </p>

  <ThinkingBox title="Bell 不等式之后：非定域性的量化">
    <p>
      Bell 不等式的违反告诉我们量子关联是"非经典的"，但这只是第一步。
      现代研究进一步追问：非定域性能否被量化？不同形式的量子关联（纠缠、Bell 非定域性、
      量子失协）之间有何区别？存在纠缠但不违反任何 Bell 不等式的量子态
      （Werner 态在某些参数范围内即为此例），说明纠缠是比 Bell 非定域性更弱的条件。
      这些问题推动了量子信息理论中关于"量子关联层级"的深入研究，
      也与量子计算的优越性（quantum advantage）密切相关。
    </p>
  </ThinkingBox>

  <ReferenceBox>
    <ul>
      <li>Bell, <em>Physics</em> <strong>1</strong>, 195 (1964)：Bell 原始论文</li>
      <li>Clauser, Horne, Shimony, Holt, <em>Phys. Rev. Lett.</em> <strong>23</strong>, 880 (1969)：CHSH 不等式</li>
      <li>Aspect, Dalibard, Roger, <em>Phys. Rev. Lett.</em> <strong>49</strong>, 1804 (1982)：Aspect 实验</li>
      <li>Hensen <em>et al.</em>, <em>Nature</em> <strong>526</strong>, 682 (2015)：首个无漏洞 Bell 实验</li>
      <li>Sakurai, <em>Modern Quantum Mechanics</em>, Ch.3.9：Bell 不等式</li>
      <li>Nielsen &amp; Chuang, <em>Quantum Computation and Quantum Information</em>, Ch.2.6：Bell 不等式与量子信息</li>
    </ul>
  </ReferenceBox>

  <!-- ================================================================== -->
  <!-- 10.3 量子信息初步 -->
  <!-- ================================================================== -->
  <h2 id="quantum-info">10.3 量子信息初步</h2>

  <h3>量子比特与 Bloch 球</h3>

  <p>
    量子信息的基本单元是<strong>量子比特</strong>（qubit），即一个二维 Hilbert 空间中的态。
    选定计算基 <Fragment set:html={defined.ketZero} /> 和 <Fragment set:html={defined.ketOne} />
    （例如自旋的 $|\!\uparrow\rangle$ 和 $|\!\downarrow\rangle$），
    一般的量子比特态为
  </p>

  <Fragment set:html={defined.qubitState} />

  <p>
    与经典比特（只能取 $0$ 或 $1$）不同，量子比特可以处于基态的任意<strong>叠加</strong>。
    除去全局相位（不可观测），归一化的量子比特态可以参数化为
  </p>

  <Fragment set:html={defined.blochSphere} />

  <p>
    其中 $\theta \in [0, \pi]$，$\varphi \in [0, 2\pi)$。
    这定义了单位球面上的一个点——<strong>Bloch 球</strong>。
    Bloch 向量 <Fragment set:html={defined.blochVector} />
    指向球面上对应的点。纯态对应 Bloch 球面上的点（<Fragment set:html={defined.blochPure} />），
    混合态对应球内部的点（<Fragment set:html={defined.blochMixed} />），
    完全混合态 $\hat{\rho} = \hat{I}/2$ 位于球心（<Fragment set:html={defined.blochCenter} />）。
    一般的单量子比特密度矩阵为
  </p>

  <Fragment set:html={defined.densityMixed} />

  <InsightBox title="Bloch 球的几何意义">
    <p>
      Bloch 球提供了量子比特的完整几何图像。球面上对径的两点对应正交态；
      绕某轴的旋转对应 $SU(2)$ 幺正变换（量子门）。
      例如，绕 $z$ 轴旋转角 $\phi$ 对应相位门 $R_z(\phi) = e^{-i\phi\hat{\sigma}_z/2}$，
      绕 $y$ 轴旋转对应 $R_y(\theta) = e^{-i\theta\hat{\sigma}_y/2}$。
      任何单量子比特门都可以分解为绕不同轴的旋转的组合。
      需要注意的是，Bloch 球只对单个量子比特有效——
      多量子比特态（纠缠态）的几何远比单个 Bloch 球复杂得多。
    </p>
  </InsightBox>

  <h3>量子不可克隆定理</h3>

  <p>
    经典信息可以任意复制，但量子信息有一个根本性的限制——
    <strong>量子不可克隆定理</strong>（Wootters &amp; Zurek, 1982; Dieks, 1982）：
    不存在一个幺正操作能够将未知量子态完美复制。
  </p>

  <Derivation title="不可克隆定理的证明">
    <p>
      假设存在一个幺正算符 $\hat{U}$，作用在待克隆态和一个"空白"辅助态 <Fragment set:html={defined.ketZero} /> 上，
      能够实现克隆：
    </p>
    <Fragment set:html={defined.cloningU1} />
    <p>
      如果这对所有态都成立，则对另一个态 <Fragment set:html={defined.cloningPhi} /> 也有
    </p>
    <Fragment set:html={defined.cloningU2} />
    <p>
      取两个等式的内积。左端：由于 $\hat{U}$ 是幺正的（保持内积），
    </p>
    $$\langle\phi|\langle 0|\hat{U}^\dagger\hat{U}|\psi\rangle|0\rangle = \langle\phi|\psi\rangle\langle 0|0\rangle = \langle\phi|\psi\rangle$$
    <p>
      右端：
    </p>
    $$\langle\phi|\phi\rangle\langle\phi|\psi\rangle\langle\psi|\psi\rangle = \langle\phi|\psi\rangle^2$$
    <p>
      （假设态已归一化。）因此得到
    </p>
    <Fragment set:html={defined.cloningInner} />
    <p>
      即 <Fragment set:html={defined.cloningContradiction} />。
      这意味着 $\langle\phi|\psi\rangle = 0$ 或 $\langle\phi|\psi\rangle = 1$——
      也就是说，$|\psi\rangle$ 和 $|\phi\rangle$ 必须相同或正交。
      因此，不可能存在一个对<strong>所有</strong>量子态都有效的克隆操作。证毕。
    </p>
  </Derivation>

  <p>
    不可克隆定理有深远的物理意义：它保证了量子密钥分发（QKD）的安全性——
    窃听者无法在不引起扰动的情况下复制量子信道上传输的量子态。
    同时，它也意味着量子信息不能像经典信息那样随意备份，
    这是量子纠错理论面临的根本挑战之一。
  </p>

  <h3>量子隐形传态</h3>

  <p>
    量子隐形传态（quantum teleportation, Bennett <em>et al.</em>, 1993）是量子纠缠作为
    物理资源的最典型范例。它允许利用预先共享的纠缠态和经典通信，
    将一个未知量子态从 Alice 传送到 Bob——无需传输携带该量子态的物理粒子。
  </p>

  <p>
    <strong>协议</strong>：Alice 持有要传送的量子态 $|\psi\rangle_1 = \alpha|0\rangle + \beta|1\rangle$
    和纠缠对的粒子 2，Bob 持有纠缠对的粒子 3。初始共享态为 <Fragment set:html={defined.singletInline} />
    （粒子 2 和 3）。三粒子总态为
  </p>

  <Fragment set:html={defined.teleportInitial} />

  <p>
    关键步骤是将粒子 1 和 2 的态用 Bell 基展开。利用计算基与 Bell 基之间的变换关系
  </p>

  <Fragment set:html={defined.bellBasis} />
  <Fragment set:html={defined.bellBasis2} />

  <p>
    将三粒子态改写为
  </p>

  <Fragment set:html={defined.teleportExpand} />

  <p>
    Alice 对粒子 1 和 2 执行 Bell 基测量（四种结果等概率出现，每种概率 $1/4$）。
    根据测量结果，Bob 的粒子 3 将坍缩到以下四种态之一：
  </p>

  <ul>
    <li>$|\Psi^-\rangle_{12}$：粒子 3 处于 $-(\alpha|0\rangle + \beta|1\rangle)$，即 $-|\psi\rangle$（差一个全局相位）</li>
    <li>$|\Psi^+\rangle_{12}$：粒子 3 处于 $-\alpha|0\rangle + \beta|1\rangle$，需要 $\hat{\sigma}_z$ 修正</li>
    <li>$|\Phi^-\rangle_{12}$：粒子 3 处于 $\alpha|1\rangle + \beta|0\rangle$，需要 $\hat{\sigma}_x$ 修正</li>
    <li>$|\Phi^+\rangle_{12}$：粒子 3 处于 $\alpha|1\rangle - \beta|0\rangle$，需要 $i\hat{\sigma}_y$ 修正</li>
  </ul>

  <p>
    Alice 通过经典信道将她的 2 比特测量结果告知 Bob。
    Bob 根据信息对粒子 3 施加相应的 Pauli 修正操作
    （<Fragment set:html={defined.pauliCorrections} /> 之一），
    就能将粒子 3 的态恢复为 <Fragment set:html={defined.teleportResult} />。
  </p>

  <InsightBox title="隐形传态不违反相对论">
    <p>
      量子隐形传态<strong>不是</strong>超光速通信。Alice 的 Bell 测量瞬间使 Bob 的粒子坍缩，
      但 Bob 在收到 Alice 的经典信息（以光速或更低速度传播）之前，
      无法区分他的粒子处于哪个态——Bob 的粒子 3 的约化密度矩阵始终是 $\hat{I}/2$，
      与 Alice 的测量选择无关。只有当 Bob 知道 Alice 的测量结果后，
      他才能执行正确的 Pauli 修正来恢复 $|\psi\rangle$。
      因此，可用信息的传递速度不超过光速，与相对论完全一致。
      量子隐形传态所展示的是：纠缠可以作为一种<strong>资源</strong>，
      配合经典通信实现经典手段无法完成的任务。
    </p>
  </InsightBox>

  <h3>量子纠缠作为资源</h3>

  <p>
    从量子信息的角度，纠缠不仅是一种奇特的物理现象，更是一种可以被消耗和利用的<strong>资源</strong>。
    纠缠态可以用于：
  </p>

  <ul>
    <li><strong>量子隐形传态</strong>：一对最大纠缠态 + 2 比特经典通信 = 传送一个量子比特</li>
    <li><strong>超密编码</strong>（superdense coding）：一对最大纠缠态 + 传送一个量子比特 = 2 比特经典信息</li>
    <li><strong>量子密钥分发</strong>（QKD）：纠缠的非定域关联保证密钥安全性</li>
    <li><strong>量子计算</strong>：纠缠是量子并行性和量子加速的核心资源</li>
  </ul>

  <p>
    纠缠的度量可以用 Schmidt 分解来刻画。对于纯态双体系统，
    总存在一组正交基使得态可以写成 Schmidt 分解的形式：
  </p>

  <Fragment set:html={defined.schmidtDecomp} />

  <p>
    Schmidt 系数 $\lambda_i$ 的分布刻画了纠缠的程度。当只有一个非零 Schmidt 系数时，
    态是可分的（无纠缠）；当所有 Schmidt 系数相等时，态是最大纠缠的。
    纠缠的定量度量可以用纠缠熵（von Neumann 熵的约化密度矩阵版本）来给出：
  </p>

  <Fragment set:html={defined.entanglementEntropy} />

  <h3>退相干：从纯态到混合态</h3>

  <p>
    实际的量子系统不可能完全与环境隔离。当量子系统与环境发生相互作用时，
    系统-环境整体处于一个纠缠态：
  </p>

  <Fragment set:html={defined.systemEnv} />

  <p>
    如果我们只关心系统本身（对环境自由度取迹），系统的约化密度矩阵为
  </p>

  <Fragment set:html={defined.reducedDensity} />

  <p>
    当环境态随着系统态的不同而变得正交（<Fragment set:html={defined.decoherence} />）时——
    这在宏观环境中由于自由度极多而极快发生——密度矩阵的非对角元（相干项）消失：
  </p>

  <Fragment set:html={defined.decoherentRho} />

  <p>
    这就是<strong>退相干</strong>（decoherence）过程。它将量子叠加态转化为经典的统计混合态——
    密度矩阵变为对角的，不再有量子干涉效应。退相干不需要"测量"或"观测者"——
    与环境的自然耦合就足以使宏观物体的量子相干性在极短的时间尺度内（对于宏观物体，
    通常为 $10^{-20}$ 秒量级）消失。
  </p>

  <p>
    退相干的度量可以用 von Neumann 熵来刻画：<Fragment set:html={defined.vonNeumann} />。
    纯态的熵为零（<Fragment set:html={defined.pureEntropy} />），
    $d$ 维 Hilbert 空间中最大混合态的熵为 <Fragment set:html={defined.maxEntropy} />。
    退相干过程中，系统的 von Neumann 熵从零单调增加，反映了量子信息从系统向环境的不可逆流失。
  </p>

  <ThinkingBox title="退相干与量子-经典过渡">
    <p>
      退相干理论（Zurek, 1981; Joos &amp; Zeh, 1985）为理解量子力学与经典世界之间的关系提供了关键洞察。
      经典世界的"确定性"并非因为量子力学在宏观尺度上失效，
      而是因为宏观系统与环境的纠缠导致了极其迅速的退相干——
      使得宏观叠加态在任何可行的实验中都无法观测到。
      退相干选择出一组"指针态"（pointer states），即在与环境耦合下最稳定的态，
      这些态对应于经典可观测量的本征态。
      然而，退相干本身<strong>并不解决</strong>量子测量问题——
      它解释了为什么我们看不到"Schrodinger 猫"，但不能解释为什么我们最终观测到一个确定的结果。
      后者涉及量子力学的解释问题（多世界、退相干历史、QBism 等），至今仍是开放的哲学议题。
    </p>
  </ThinkingBox>

  <Derivation title="退相干时间尺度的估计">
    <p>
      考虑一个超导量子比特（典型能级间距 $\sim$ 5 GHz）与电磁环境耦合。
      退相干时间 $T_2$ 由环境涨落的谱密度决定。
      对于欧姆型环境（$J(\omega) \propto \omega$），在热涨落为主的高温极限下：
    </p>
    $$\frac{1}{T_2} \sim \frac{\alpha k_B \mathcal{T}}{\hbar}$$
    <p>
      其中 $\alpha$ 是无量纲耦合强度。对于宏观物体（尘埃颗粒与空气分子碰撞），
      退相干速率可以估计为
    </p>
    $$\Gamma_{\text{dec}} \sim n v \sigma \left(\frac{\Delta x}{\lambda_{\text{dB}}}\right)^2$$
    <p>
      其中 $n$ 是气体分子数密度，$v$ 是热速度，$\sigma$ 是碰撞截面，
      $\Delta x$ 是叠加态的空间尺度，$\lambda_{\text{dB}}$ 是气体分子的 de Broglie 波长。
      对于室温大气中的微米量级粒子，退相干速率可达 $10^{20}\;\text{s}^{-1}$ 以上——
      量子叠加在 $10^{-20}$ 秒内就消失了。
      这解释了为什么宏观世界看起来是"经典的"。
    </p>
  </Derivation>

  <ReferenceBox>
    <ul>
      <li>Nielsen &amp; Chuang, <em>Quantum Computation and Quantum Information</em>, Ch.1-2, Ch.12：量子比特、隐形传态、量子信道</li>
      <li>Sakurai, <em>Modern Quantum Mechanics</em>, Ch.3.9-3.10：纠缠与 Bell 不等式</li>
      <li>Preskill, <em>Quantum Computation</em> (Caltech 讲义), Ch.1-4：量子信息基础</li>
      <li>Zurek, <em>Rev. Mod. Phys.</em> <strong>75</strong>, 715 (2003)：退相干与量子-经典过渡</li>
      <li>Weinberg, <em>Lectures on Quantum Mechanics</em>, Ch.12：量子力学的解释问题</li>
    </ul>
  </ReferenceBox>

</ChapterLayout>
